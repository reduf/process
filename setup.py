from setuptools import setup

setup(name='process',
    version='1.0',
    description='Python module to interact with running processes on Windows',
    author='Laurent Dufresne',
    license='MIT',
    py_modules=['process'],
    url = 'https://github.com/reduf/process'
)
